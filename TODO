implement getotps for command line arguments
add an option to exit the script at the point it would normally spawn the shell
add an option to clean up an existing netns
add DEVICE verification
add option to determine if network-manager service will be reset before exiting
assess input verification needs
